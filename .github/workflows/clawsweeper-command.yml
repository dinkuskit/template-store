name: ClawSweeper command

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  review:
    uses: dinkuskit/.github/.github/workflows/clawsweeper-command.yml@main
